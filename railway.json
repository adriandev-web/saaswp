{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd woolanding-frontend && npm install && npm run build && cd ../woolanding-backend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd woolanding-backend && node dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
